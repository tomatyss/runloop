[package]
name = "runloop-router"
version = "0.1.0"
edition = "2024"

[lib]
name = "runloop_router"

[dependencies]
once_cell = "1.19"
regex = "1.10"
runloop-core = { path = "../core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
