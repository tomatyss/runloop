[package]
name = "runloop-sdk"
version = "0.1.0"
edition = "2024"

[lib]
name = "runloop_sdk"

[dependencies]
runloop-core = { path = "../core" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
