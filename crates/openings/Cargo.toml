[package]
name = "runloop-openings"
version = "0.1.0"
edition = "2024"

[lib]
name = "runloop_openings"

[dependencies]
runloop-core = { path = "../core" }
serde = { version = "1.0", features = ["derive"] }
serde_yaml = "0.9"
thiserror = "1.0"
