[package]
name = "runloop-agents-common"
version = "0.1.0"
edition = "2024"
license.workspace = true

[dependencies]
async-trait = "0.1"
runloop-core = { path = "../../core" }
runloop-kb = { path = "../../kb" }
runloop-model-broker = { path = "../../model-broker" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"

[lints]
workspace = true
